import React from "react";


class ChatWindow extends React.Component {
    render() {
        return (
            <div className="chat-window">
                This is the chat window. But it's empty for now
            </div>
        );
    };
}


export default ChatWindow;